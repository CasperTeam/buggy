def get_default_welcome(chat, user):
    if chat.id == 1246820081:  
        return f"""
        \n**Hey** [{user.first_name}](tg://user?id={user.id}) `{user.id}`
        \n**Welcome to** {chat.title}
        \n**How are you?**
        \n** • Tap confirm button to join, else auto-removed.**
        \n** • Kindly read pinned post for latest updates. **
        \n** • Use** #faq **for FAQ's &** /notes *for any help. **
        \n** • press the button 10 seconds after and 1000 before you joined **
        """
    if chat.id == 1362482317:  
        return f"""
        \n**Hey** [{user.first_name}](tg://user?id={user.id}) `{user.id}`
        \n**Welcome to** {chat.title}
        \n**How are you?**
        \n** • Tap confirm button to join, else auto-removed.**
        \n** • Kindly read pinned post for latest updates. **
        \n** • Use** #faq **for FAQ's &** /notes *for any help. **
        \n** • press the button 10 seconds after and 1000 before you joined **
        """
   
    if chat.id == 1173851829:  
        return f"""
        \n**Hey** [{user.first_name}](tg://user?id={user.id}) `{user.id}`
        \n**Welcome to** {chat.title}
        \n**How are you?**
        \n** • Tap confirm button to join, else auto-removed.**
        \n** • Kindly read pinned post for latest updates. **
        \n** • Use** #faq **for FAQ's &** /notes *for any help. **
        \n** • press the button 10 seconds after and 1000 before you joined **
        """
    if chat.id == 1289165491:  
        return f"""
        \n**Hey** [{user.first_name}](tg://user?id={user.id}) `{user.id}`
        \n**Welcome to** {chat.title}
        \n**How are you?**
        \n** • Tap confirm button to join, else auto-removed.**
        \n** • Kindly read pinned post for latest updates. **
        \n** • Use** #faq **for FAQ's &** /notes *for any help. **
        \n** • press the button 10 seconds after and 1000 before you joined **
        """
    if chat.id == 1390067401:  
        return f"""
        \n**Hey** [{user.first_name}](tg://user?id={user.id}) `{user.id}`
        \n**Welcome to** {chat.title}
        \n**How are you?**
        \n** • Tap confirm button to join, else auto-removed.**
        \n** • Kindly read pinned post for latest updates. **
        \n** • Use** #faq **for FAQ's &** /notes *for any help. **
        \n** • press the button 10 seconds after and 1000 before you joined **
        """
    if chat.id == 1198165933:  
        return f"""
        \n**Hey** [{user.first_name}](tg://user?id={user.id}) `{user.id}`
        \n**Welcome to** {chat.title}
        \n**How are you?**
        \n** • Tap confirm button to join, else auto-removed.**
        \n** • Kindly read pinned post for latest updates. **
        \n** • Use** #faq **for FAQ's &** /notes *for any help. **
        \n** • press the button 10 seconds after and 1000 before you joined **
        """
    if chat.id == 1319484169:  
        return f"""
        \n**Hey** [{user.first_name}](tg://user?id={user.id}) `{user.id}`
        \n**Welcome to** {chat.title}
        \n**How are you?**
        \n** • Tap confirm button to join, else auto-removed.**
        \n** • Kindly read pinned post for latest updates. **
        \n** • Use** #faq **for FAQ's &** /notes *for any help. **
        \n** • press the button 10 seconds after and 1000 before you joined **
        """
    if chat.id == 1366720969:  
        return f"""
        \n**Hey** [{user.first_name}](tg://user?id={user.id}) `{user.id}`
        \n**Welcome to** {chat.title}
        \n**How are you?**
        \n** • Tap confirm button to join, else auto-removed.**
        \n** • Kindly read pinned post for latest updates. **
        \n** • Use** #faq **for FAQ's &** /notes *for any help. **
        \n** • press the button 10 seconds after and 1000 before you joined **
        """